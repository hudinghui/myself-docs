# 2026-06-12

## 今日工作
- 梳理 `voghion-marketing-push` 中 `Push / Email / SMS / WhatsApp` 四个渠道的“配置建模 -> 任务生成 -> 执行发送”链路，核对凭证迁移方案是否真正落到代码和表结构。
- 明确本轮凭证方案已经收敛为“配置表选择 `credentialId`，生成任务时把 `credentialId` 固化到任务快照，执行阶段只认任务表上的 `credentialId`”，并据此定位各渠道当前实现的偏差点。
- 在 `voghion-marketing-push` 中按上述方案调整相关实体与发送链路，收口配置实体、任务实体、模板实体和执行读取路径之间的契约。
- 复核 `voghion-comment` 中四个渠道营销任务管理相关实体类与 VO，确认管理侧模型是否满足“配置表与任务表均承载 `credentialId`、模板配置不承载执行态凭证”的方案要求。
- 阅读 CM.com `Transactions API`、`Incoming Messages`、`Incoming Status Reports`、`RCS Incoming Events` 等文档，梳理 `in / out`、RCS webhook、`reference / messageContext` 与会话计费之间的关系。
- 将上述结论合并沉淀到学习笔记、技术知识库、工程方法论与技术知识图谱，避免只停留在单次排查和文档问答中。

## 关键结论
- 多渠道凭证迁移不能只看字段命名或单表加列，必须同时核对“表结构、实体映射、任务快照写入、执行读取”四层契约是否闭合。
- 当业务前提已经明确为“一个执行任务只使用一个凭证 ID”时，配置层负责选择凭证，任务表负责冻结执行凭证，执行阶段不应再回读模板配置里的凭证。
- `voghion-comment` 管理侧四个渠道的核心配置实体、任务实体与管理 VO 已基本符合新方案，管理层不需要额外结构调整。
- CM.com / RCS 里的 `in / out` 是消息方向维度，`Basic / Single / Conversation` 是计费分类维度，两者不能互相替代。
- RCS webhook 文档没有提供明确的 `conversationId` 或“哪条 single message 转成 conversation”的专用标识；目前只能依赖 `reference`、`messageContext` 和本地 24 小时窗口规则做关联推断。
- 仅靠 webhook 原始记录难以直接得出权威账单，若要做每日费用统计，应把本地事件归并结果与供应商交易/账单接口做日级对账。

## 产出
- 更新 `/Users/edy/devople/doc/知识资产/worklog/2026-06.md`
- 更新 `/Users/edy/devople/doc/知识资产/learning/知识笔记.md`
- 更新 `/Users/edy/devople/doc/知识资产/knowledge/技术知识库.md`
- 更新 `/Users/edy/devople/doc/知识资产/knowledge/工程方法论.md`
- 更新 `/Users/edy/devople/doc/知识资产/knowledge/技术知识图谱.xmind`

# 2026-06-11

## 今日工作
- 优化每日知识资产管理提示词，补强“执行计划 -> 阶段检查 -> 最终检查”的强约束流程，并显式补齐资产路径、输出顺序和信息不足时的处理规则。
- 调研多 Agent 应用的基础能力与企业级架构分层，明确单 Agent、固定工作流与多 Agent 的适用边界，以及 MVP 的最小落地形态。
- 设计企业级共享能力层中 `Memory / Session` 与 `RAG / 向量检索` 的职责边界、分层方式和治理要求。
- 将上述内容合并沉淀到学习笔记、技术知识库、工程方法论与技术知识图谱，避免只停留在一次性问答里。
- 设计并实现 `mysql-mcp-server` 只读 MySQL MCP Server 脚手架，补齐 SQL 守卫、配置加载、`schema://tables` 资源、`query_readonly` 工具、单元测试和本地接入说明。

## 关键结论
- 长提示词自动化任务如果没有显式的阶段顺序、检查点和目标资产约束，模型很容易只覆盖前半部分要求。
- 多 Agent 应用不应一开始就追求角色数量，最稳的落地路线通常是先做 `单 Agent + 工具`，再把检索、执行、审核等职责逐步拆开。
- `Memory / Session` 是以用户和任务为中心的上下文系统，`RAG / 向量检索` 是以企业知识为中心的事实供给系统，两者应独立建模并通过编排层协同。
- 给 Agent 暴露数据库查询能力时，数据库只读权限和应用层 SQL 守卫必须同时存在，单独依赖其中一层都不够稳。
- SQL 校验、limit 收敛和配置解析适合拆成独立模块做无数据库单测，MCP wiring 与数据库连接层应保持薄封装。
- 只读数据库工具的本地接入优先级应明确为“显式配置文件路径 > 项目本地配置文件 > 环境变量默认项”，减少集成期歧义。

## 产出
- 更新 `/Users/edy/devople/doc/知识资产/worklog/2026-06.md`
- 更新 `/Users/edy/devople/doc/知识资产/learning/知识笔记.md`
- 更新 `/Users/edy/devople/doc/知识资产/knowledge/技术知识库.md`
- 更新 `/Users/edy/devople/doc/知识资产/knowledge/工程方法论.md`
- 更新 `/Users/edy/devople/doc/知识资产/knowledge/技术知识图谱.xmind`
- 新增 `/Users/edy/devople/doc/mysql-mcp-server/pyproject.toml`
- 新增 `/Users/edy/devople/doc/mysql-mcp-server/README.md`
- 新增 `/Users/edy/devople/doc/mysql-mcp-server/src/mysql_mcp/config.py`
- 新增 `/Users/edy/devople/doc/mysql-mcp-server/src/mysql_mcp/guards.py`
- 新增 `/Users/edy/devople/doc/mysql-mcp-server/src/mysql_mcp/server.py`
- 新增 `/Users/edy/devople/doc/mysql-mcp-server/tests/test_config.py`
- 新增 `/Users/edy/devople/doc/mysql-mcp-server/tests/test_guards.py`

# 2026-06-10

## 今日工作
- 梳理营销推送链路中 `task_list`、`task_config`、`task_template_config` 的职责边界，确认任务实例化与模板配置维护属于两条不同生命周期。
- 排查凭证绑定改造方案，明确 `credentialId` 的落点应由发送阶段真实读取对象决定，并保留新凭证优先、旧逻辑兜底的兼容路径。
- 复盘 Kafka 消费节点异常场景，补充消费者线程静默退出、心跳假存活、静态成员阻塞再平衡之间的因果链路。
- 将上述内容沉淀到学习笔记、技术知识库、工程方法论与技术知识图谱，避免知识只停留在单次排障记录中。

## 关键结论
- 自动生成任务的链路只负责生成任务实例，不应隐式承担模板配置补建责任；缺失模板配置时，应优先回到配置创建/同步链路定位 source of truth。
- 凭证字段应该落在发送时真正参与选择和组装的配置层；如果存在语言、模板、渠道粒度差异，过早固化到任务层会丢失真实分歧。
- Kafka 消费故障不能只盯 `poll()` 线程，要同时检查消费线程、心跳线程、消费组协调器和静态成员机制是否共同造成“假存活 + 不自愈”。

## 产出
- 新增并维护 `/Users/edy/devople/doc/知识资产/learning/知识笔记.md`
- 新增并维护 `/Users/edy/devople/doc/知识资产/knowledge/技术知识库.md`
- 新增并维护 `/Users/edy/devople/doc/知识资产/knowledge/工程方法论.md`
- 新增并维护 `/Users/edy/devople/doc/知识资产/knowledge/技术知识图谱.xmind`

# 2026-06-09

## 今日工作
- 核对 `credentialId` 字段命名与数据库列 `credential_id` 的映射关系，确认 ORM 默认命名规则下应优先服从数据库契约。
- 复盘 `task_list` 与 `task_template_config` 的职责边界，确认凭证字段的落点应由发送时真实读取对象决定，而不是按“离任务更近”拍板。
- 全链路审视多渠道发送改造，识别 `mapping` / `builder` 重建对象、凭证注入、本地校验和真实请求消费之间的契约漂移。
- 修复 WhatsApp 配置新增时子表 `configId` 默认 `0` 的问题，将主表保存后的新 ID 回填到 DTO 及其扩展资源对象。
- 抽象 AI 迁移改造场景下的字段链路审计模板，要求先列渠道清单、再做字段流表、最后按问题类型复核。

## 关键结论
- 依赖 ORM 默认映射时，Java 字段命名要优先对齐数据库真实列名，否则会自然映射到错误列。
- 凭证字段该放哪一层，要看发送入口最终读取谁；如果发送按语言或模板分流，任务表单字段不足以表达真实差异。
- 字段链路问题不能只看赋值点，要同时核对 `applier`、`validateParam` 与真实请求三层契约是否闭合。
- 新增父记录后立即创建子记录时，必须先把生成的主键回写到内存对象，再组装子记录，否则容易出现关联字段默认值或空值。
- 让 AI 做迁移改造时，必须把任务改写成“可枚举、可证明、可复核”的审计格式，才能显著降低漏改和漏检概率。

## 产出
- 维护 `/Users/edy/devople/doc/知识资产/worklog/2026-06.md`
- 更新 `/Users/edy/devople/doc/知识资产/learning/知识笔记.md`
- 更新 `/Users/edy/devople/doc/知识资产/knowledge/技术知识库.md`
- 更新 `/Users/edy/devople/doc/知识资产/knowledge/工程方法论.md`
